##################
# Puppet Managed #
##################
#
# Highest priority to stable
Package: *
Pin: release n=buster,o=Debian
Pin-Priority: 700

<% pfw = scope.lookupvar('::packages_from_bullseye')
   pfw = [] unless pfw.is_a?(Array)
   pfw.uniq.each do |package|
-%>
# we want <%=package%> from bullseye
Package: <%=package%>
Pin: release n=bullseye,o=Debian
Pin-Priority: 700

<% end -%>
