<h2><%=@node.fqdn%></h2>

<h3>Add new domain</h3>

<%= labelled_form_for(:webserver1_domain, @domain, :url=>{:action=>'add_domain'}, :html=>{:class=>'tabular'}) do |f| -%>
  <p><%= error_messages_for 'domain' %></p>
  <%= render :partial => 'webserver1_domain', :locals => {:form => f} %>
  <p>
  <%= submit_tag("Add")%>
  </p>
<% end -%>

<%= link_to "Cancel", { :action => 'configure', :id => @klass } %>
