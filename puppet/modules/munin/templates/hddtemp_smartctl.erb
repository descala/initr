[hddtemp_smartctl]
user root
env.drives <% drives[0].each do |dev| %><%= dev %> <% end %>
<%
i=0
drives[0].each do |dev| %>
env.type_<%= dev %> <%= drives[1][i] %>
<%= (!drives[2].nil? and !drives[2][i].empty?) ? "env.args_"+drives[1][i]+" "+drives[2][i] : "" %>
<%
i=i+1
end %>
