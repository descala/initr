##########################
#     Puppet managed     #
##########################
#
# see /var/spool/vacation/vacation.pl
$db_type = '<%=scope.lookupvar('::db_driver')%>';
# leave empty for connection via UNIX socket
$db_host = '';
$db_username = '<%=db_user%>';
$db_password = '<%=db_passwd%>';
$vacation_domain = 'autoreply.localhost';
$log_to_file = 1;
$log_level = 1;
