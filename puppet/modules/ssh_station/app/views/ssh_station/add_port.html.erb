<h2><%=@node.fqdn%></h2>

<h3>Add new port redirection</h3>

<%= form_for @port,:url => {:action => 'add_port'}, :html=>{:class=>'tabular'} do |f| -%>
  <p><%= error_messages_for 'port' %></p>
  <%= render :partial => 'ssh_station_port', :locals => {:form => f} %>
  <p>
  <%= submit_tag("Add")%>
  </p>
<% end -%>

<%= link_to "Cancel", { :action => 'configure', :id => @klass } %>
