<%= klass_menu("Editing #{@klass.name}") %>

<p><%= "Needed attributes for this class:" %></p>

<% form_for :custom_klass, @klass, :url => ({ :action=>'configure', :id=>@klass }) do |f| %>

  <%= render :partial => f %>

  <%=submit_tag 'Save'%>

<% end %>

<%= link_to 'Back', :back -%>
