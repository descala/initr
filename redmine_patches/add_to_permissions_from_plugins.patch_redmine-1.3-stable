diff --git a/lib/redmine/access_control.rb b/lib/redmine/access_control.rb
index 7f3b22d..f5a3514 100644
--- a/lib/redmine/access_control.rb
+++ b/lib/redmine/access_control.rb
@@ -94,6 +94,10 @@ module Redmine
         @public = options[:public] || false
         @require = options[:require]
         @project_module = options[:project_module]
+        add_actions(hash)
+      end
+
+      def add_actions(hash)
         hash.each do |controller, actions|
           if actions.is_a? Array
             @actions << actions.collect {|action| "#{controller}/#{action}"}
diff --git a/lib/redmine/plugin.rb b/lib/redmine/plugin.rb
index 2c77cf0..7c12f9a 100644
--- a/lib/redmine/plugin.rb
+++ b/lib/redmine/plugin.rb
@@ -221,6 +221,10 @@ module Redmine #:nodoc:
       end
     end
 
+    def add_permission(name, actions)
+      AccessControl.permission(name).add_actions(actions) unless AccessControl.permission(name).nil?
+    end
+
     # Defines a project module, that can be enabled/disabled for each project.
     # Permissions defined inside +block+ will be bind to the module.
     #
